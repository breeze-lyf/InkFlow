# 墨流领域词汇

代码、测试、界面和文档统一使用以下词义：

- **文档库**：用户主动打开的本地文件夹；文件树、快速打开和监听都以它为边界。
- **文稿**：可编辑并保存到 Markdown 文件的内容；尚无磁盘路径时称“未命名文稿”。
- **页签**：一个已打开文稿或只读预览的界面与运行时状态；不等同于磁盘文件。
- **恢复草稿**：为防止异常退出丢字而保存的临时内容快照；成功保存或明确放弃后应清理，不是正式文稿副本。
- **外部修改冲突**：文稿打开后，磁盘内容被其他程序修改或删除，同时墨流内仍有未保存内容的状态；必须让用户选择，不能静默覆盖任一方。
- **资源服务**：仅监听本机回环地址、把已授权本地媒体提供给编辑器预览的临时 HTTP 服务；不是公开文件服务器。
- **导出管线**：从当前渲染内容收集样式与资源，再生成 PDF、Word、PNG 长图或自包含 HTML 的完整路径。
- **功能冒烟**：启动真实 Electron 界面并自动执行核心工作流的端到端回归；当前发布基线为 `44/44`。

“文件”用于描述文件系统对象，“文档”只在泛指内容或外部格式时使用。用户可见文案优先使用“文稿”和“文档库”。
